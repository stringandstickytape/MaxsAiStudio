<Window x:Class="AiStudio4.McpStandalone.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Settings" Height="500" Width="650" MinHeight="450" MinWidth="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MCP Server Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- YouTube API Key -->
                <GroupBox Header="YouTube API" Margin="0,0,0,10">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Column="0" Content="API Key:" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" Name="YouTubeApiKeyBox" VerticalAlignment="Center" Margin="5,0"/>
                        <Button Grid.Column="2" Content="Show" Name="ShowYouTubeKeyButton" 
                                Click="ShowYouTubeKey_Click" Margin="5,0,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- Azure DevOps PAT -->
                <GroupBox Header="Azure DevOps" Margin="0,0,0,10">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Column="0" Content="PAT:" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" Name="AzureDevOpsPATBox" VerticalAlignment="Center" Margin="5,0"/>
                        <Button Grid.Column="2" Content="Show" Name="ShowAzurePatButton" 
                                Click="ShowAzurePat_Click" Margin="5,0,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- GitHub Token -->
                <GroupBox Header="GitHub" Margin="0,0,0,10">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Column="0" Content="Token:" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" Name="GitHubTokenBox" VerticalAlignment="Center" Margin="5,0"/>
                        <Button Grid.Column="2" Content="Show" Name="ShowGitHubTokenButton" 
                                Click="ShowGitHubToken_Click" Margin="5,0,0,0"/>
                    </Grid>
                </GroupBox>

                <!-- Working Directory -->
                <GroupBox Header="Working Directory" Margin="0,0,0,10">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Column="0" Content="Path:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Name="ProjectPathBox" VerticalAlignment="Center" Margin="5,0"/>
                        <Button Grid.Column="2" Content="Browse..." Name="BrowseButton" 
                                Click="Browse_Click" Margin="5,0,0,0"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <ui:Button Content="Save" Width="100" Height="35" Margin="0,0,10,0" 
                      Click="Save_Click" Appearance="Primary"
                      Icon="{ui:SymbolIcon Save24}"/>
            <ui:Button Content="Cancel" Width="100" Height="35" 
                      Click="Cancel_Click"
                      Icon="{ui:SymbolIcon Dismiss24}"/>
        </StackPanel>
    </Grid>
</Window>