<Page x:Class="AiStudio4.McpStandalone.Pages.ServerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Server">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Server Management" 
                      FontSize="28" 
                      FontWeight="Bold"
                      Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="Manage your MCP server configuration and status" 
                      FontSize="14" 
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Server Status Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- MCP Server Status -->
            <ui:Card Grid.Column="0" Margin="0,0,12,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="Server24" 
                                      Margin="0,0,12,0"
                                      FontSize="24"/>
                        <TextBlock Text="MCP Server" 
                                  FontSize="20" 
                                  FontWeight="SemiBold"
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <ui:InfoBar Grid.Row="1"
                               IsOpen="True" 
                               IsClosable="False"
                               Severity="{Binding IsMcpServerRunning, Converter={StaticResource BoolToSeverityConverter}}"
                               Title="Status"
                               Message="{Binding McpServerStatus}"
                               Margin="0,0,0,12"/>
                    
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Server Details" 
                                  FontWeight="SemiBold"
                                  Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Margin="0,2"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedServer.Name}" Margin="8,2"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Port:" Margin="0,2"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding McpServerPort}" Margin="8,2"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Status:" Margin="0,2"/>
                            <CheckBox Grid.Row="2" Grid.Column="1" 
                                     Content="Enabled" 
                                     IsChecked="{Binding SelectedServer.IsEnabled}"
                                     Margin="8,2"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </ui:Card>

            <!-- OAuth Server Status -->
            <ui:Card Grid.Column="1" Margin="12,0,0,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="Shield24" 
                                      Margin="0,0,12,0"
                                      FontSize="24"/>
                        <TextBlock Text="OAuth Server" 
                                  FontSize="20" 
                                  FontWeight="SemiBold"
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <ui:InfoBar Grid.Row="1"
                               IsOpen="True" 
                               IsClosable="False"
                               Severity="{Binding IsOAuthServerRunning, Converter={StaticResource BoolToSeverityConverter}}"
                               Title="Status"
                               Message="{Binding OAuthServerStatus}"
                               Margin="0,0,0,12"/>
                    
                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Authentication Details" 
                                  FontWeight="SemiBold"
                                  Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Port:" Margin="0,2"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OAuthServerPort}" Margin="8,2"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" Margin="0,2"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="OAuth 2.0" Margin="8,2"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </ui:Card>
        </Grid>

        <!-- Server Actions -->
        <ui:Card Grid.Row="2">
            <StackPanel Margin="20">
                <TextBlock Text="Server Actions" 
                          FontSize="18" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,16"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <ui:Button Grid.Column="0"
                              Content="Restart Server" 
                              Command="{Binding RestartServerCommand}"
                              Icon="{ui:SymbolIcon ArrowClockwise24}"
                              Appearance="Primary"
                              Margin="0,0,8,0"
                              IsEnabled="{Binding IsMcpServerRunning}"/>
                    
                    <ui:Button Grid.Column="1"
                              Content="View Logs" 
                              Icon="{ui:SymbolIcon DocumentText24}"
                              Margin="0,0,8,0"/>
                </Grid>
                
                <!-- Restart notification -->
                <ui:InfoBar IsOpen="{Binding NeedsRestart}" 
                           IsClosable="False"
                           Severity="Warning"
                           Title="Restart Required"
                           Message="{Binding RestartMessage}"
                           Margin="0,16,0,0"/>
                
                <!-- Reconnect info -->
                <ui:InfoBar IsOpen="{Binding ShowReconnectInfo}"
                           IsClosable="True"
                           Severity="Success"
                           Title="Server Restarted"
                           Message="{Binding ReconnectInfoMessage}"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>