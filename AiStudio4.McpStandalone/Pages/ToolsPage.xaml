<Page x:Class="AiStudio4.McpStandalone.Pages.ToolsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Tools">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Tools" 
                      FontSize="28" 
                      FontWeight="Bold"
                      Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="Manage which tools are available through the MCP server" 
                      FontSize="14" 
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search Box -->
        <ui:Card Grid.Row="1" Margin="0,0,0,16" Padding="16">
            <ui:TextBox PlaceholderText="Search tools..." 
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                       Icon="{ui:SymbolIcon Search24}"/>
        </ui:Card>

        <!-- Tools List -->
        <ui:Card Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Border Grid.Row="0" 
                        Padding="20,16"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Available Tools" 
                                      FontSize="16" 
                                      FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding AvailableTools.Count, StringFormat='{}{0} tools available'}" 
                                      FontSize="12" 
                                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                      Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select All/None buttons -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ui:Button Content="Select All" 
                                      Click="SelectAll_Click"
                                      Appearance="Secondary"
                                      Margin="0,0,8,0"/>
                            <ui:Button Content="Select None" 
                                      Click="SelectNone_Click"
                                      Appearance="Secondary"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Tools List -->
                <ScrollViewer Grid.Row="1" 
                             VerticalScrollBarVisibility="Auto"
                             Padding="16">
                    <ItemsControl ItemsSource="{Binding AvailableTools}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:CardAction Margin="4,2" 
                                             IsChevronVisible="False">
                                    <Grid Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <ui:ToggleSwitch Grid.Column="0" 
                                                        IsChecked="{Binding IsSelected}"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,16,0"/>
                                        
                                        <StackPanel Grid.Column="1" 
                                                   VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" 
                                                      FontSize="14"
                                                      FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Description}" 
                                                      FontSize="12" 
                                                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                      TextWrapping="Wrap"
                                                      Margin="0,2,0,0"/>
                                        </StackPanel>
                                        
                                        <ui:Badge Grid.Column="2" 
                                                 Content="{Binding Category}"
                                                 Appearance="Secondary"
                                                 VerticalAlignment="Center"
                                                 Margin="16,0,0,0"/>
                                    </Grid>
                                </ui:CardAction>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Footer with restart notification or reconnect info -->
                <Border Grid.Row="2" 
                        Padding="20,16"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="0,1,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding NeedsRestart}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ShowReconnectInfo}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Restart needed message -->
                        <StackPanel Grid.Column="0" 
                                   Orientation="Horizontal"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding NeedsRestart, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ui:SymbolIcon Symbol="Warning24" 
                                          Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                          Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding RestartMessage}" 
                                      VerticalAlignment="Center"
                                      Foreground="{DynamicResource SystemFillColorCautionBrush}"/>
                        </StackPanel>
                        
                        <!-- Reconnect info after restart -->
                        <StackPanel Grid.Column="0"
                                   Orientation="Horizontal"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding ShowReconnectInfo, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ui:SymbolIcon Symbol="CheckmarkCircle24" 
                                          Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                          Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding ReconnectInfoMessage}" 
                                      VerticalAlignment="Center"
                                      Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                        </StackPanel>
                        
                        <ui:Button Grid.Column="1"
                                  Content="Restart Server" 
                                  Command="{Binding RestartServerCommand}"
                                  Icon="{ui:SymbolIcon ArrowClockwise24}"
                                  Appearance="Primary"
                                  IsEnabled="{Binding IsMcpServerRunning}"
                                  Visibility="{Binding NeedsRestart, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>
            </Grid>
        </ui:Card>
    </Grid>
</Page>