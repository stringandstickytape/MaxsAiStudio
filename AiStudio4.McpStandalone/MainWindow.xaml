<ui:FluentWindow x:Class="AiStudio4.McpStandalone.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AiStudio4.McpStandalone"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="MCP Server Manager" 
        Height="800" 
        Width="1400"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="MCP Server Manager" ShowMaximize="True"/>

        <!-- Menu Bar -->
        <Menu Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}">
            <MenuItem Header="_File">
                <MenuItem Header="_Settings..." Click="Settings_Click">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click">
                    <MenuItem.Icon>
                        <ui:SymbolIcon Symbol="ArrowExit20"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Server Status -->
            <ui:Card Grid.Column="0" Margin="12,12,6,12">
                <StackPanel Margin="20">
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                  Text="MCP Server" 
                                  FontSize="18" 
                                  FontWeight="SemiBold"/>
                        <ui:Button Grid.Column="1" 
                                  Content="Settings"
                                  Icon="{ui:SymbolIcon Settings24}"
                                  Click="Settings_Click"/></Grid>
                    
                    <ui:InfoBar IsOpen="True" 
                               IsClosable="False"
                               Severity="{Binding IsMcpServerRunning, Converter={StaticResource BoolToSeverityConverter}}"
                               Title="Server Status"
                               Message="{Binding McpServerStatus}"
                               Margin="0,0,0,12"/>
                    
                    <StackPanel Margin="0,12,0,0">
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ui:SymbolIcon Grid.Column="0" 
                                          Symbol="Server24" 
                                          Margin="0,0,12,0"
                                          VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SelectedServer.Name}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SelectedServer.Description}" 
                                          FontSize="11" 
                                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Grid>
                        
                        <CheckBox Content="Enabled" 
                                 IsChecked="{Binding SelectedServer.IsEnabled}"
                                 Margin="0,8,0,0"/>
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <!-- Center Panel - Configuration -->
            <ui:Card Grid.Column="1" Margin="6,12">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <TextBlock Text="Server Configuration" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

                        <!-- Basic Configuration -->
                        <Expander IsExpanded="True" Margin="0,0,0,12">
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Settings24" Margin="0,0,8,0"/>
                                    <TextBlock Text="Basic Configuration" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Expander.Header>
                            <StackPanel Margin="32,12,0,0">
                                <ui:TextBox PlaceholderText="Server Name" 
                                           Text="{Binding SelectedServer.Name}"
                                           Icon="{ui:SymbolIcon Tag24}"
                                           Margin="0,0,0,12"/>
                                <ui:TextBox PlaceholderText="Description" 
                                           Text="{Binding SelectedServer.Description}"
                                           Icon="{ui:SymbolIcon Info24}"
                                           Margin="0,0,0,12"/>
                            </StackPanel>
                        </Expander>

                        <!-- Claude Code Installation -->
                        <Expander IsExpanded="True" Margin="0,0,0,12">
                            <Expander.Header>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Code24" Margin="0,0,8,0"/>
                                    <TextBlock Text="Claude Code Installation" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Expander.Header>
                            <StackPanel Margin="32,12,0,0">
                                <ui:InfoBar IsOpen="True" 
                                           IsClosable="False"
                                           Severity="Success"
                                           Title="Install MCP Server in Claude Code"
                                           Message="Copy and run this command in Claude Code to connect to this MCP server"
                                           Margin="0,0,0,12"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ui:TextBox Grid.Column="0" 
                                               Text="{Binding ClaudeInstallCommand, Mode=OneWay}"
                                               IsReadOnly="True"
                                               Icon="{ui:SymbolIcon Code24}"
                                               FontFamily="Cascadia Code, Consolas, monospace"/>
                                    <ui:Button Grid.Column="1" 
                                              Content="Copy"
                                              Icon="{ui:SymbolIcon Copy24}"
                                              Margin="8,0,0,0"
                                              Click="CopyClaudeCommand_Click"/>
                                </Grid>
                            </StackPanel>
                        </Expander>

                    </StackPanel>
                </ScrollViewer>
            </ui:Card>

            <!-- Right Panel - Tool Selection -->
            <ui:Card Grid.Column="2" Margin="6,12,12,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                              Text="Available Tools" 
                              FontSize="16" 
                              FontWeight="SemiBold" 
                              Margin="12,12,12,8"/>

                    <ui:TextBox Grid.Row="1" 
                               PlaceholderText="Search tools..." 
                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                               Icon="{ui:SymbolIcon Search24}"
                               Margin="12,0,12,8"/>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding AvailableTools}" Margin="8,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ui:CardAction Margin="4,2" IsChevronVisible="False">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" 
                                                     IsChecked="{Binding IsSelected}"
                                                     VerticalAlignment="Center"
                                                     Margin="8,0,12,0"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Description}" 
                                                          FontSize="11" 
                                                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                          TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <ui:Badge Grid.Column="2" 
                                                     Content="{Binding Category}"
                                                     Appearance="Secondary"
                                                     Margin="8,0"/>
                                        </Grid>
                                    </ui:CardAction>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </ui:Card>
        </Grid>

        <!-- Status Bar -->
        <ui:Card Grid.Row="3" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="{Binding IsOAuthServerRunning, Converter={StaticResource BoolToIconConverter}}" 
                                  Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding OAuthServerStatus}" Margin="0,0,16,0"/>
                    
                    <ui:SymbolIcon Symbol="{Binding IsMcpServerRunning, Converter={StaticResource BoolToIconConverter}}" 
                                  Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding McpServerStatus}" FontWeight="SemiBold"/>
                </StackPanel>

            </Grid>
        </ui:Card>
    </Grid>
</ui:FluentWindow>